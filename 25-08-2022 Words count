#include<stdio.h>
void main()
{
    char s[1000];
    int i,n,c=0;
    scanf("%[^\n]s",s);
    n=strlen(s);
    for(i=0;i<n;i++)
    {
        if(s[i]==' ') c++;
    }
    printf("The no. of words is %d",c+1);
}
