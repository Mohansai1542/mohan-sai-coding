#include<stdio.h>
int fact(n)
{
    if(n==0) return(1);
    else if(n<0) return(0);
    else return(n*fact(n-1));
}
void main()
{
    int n,ans;
    scanf("%d",&n);
    ans=fact(n);
    printf("%d",ans);
}
